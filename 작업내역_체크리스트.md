# Spring Security 기반 보안 게시판 — 작업내역 체크리스트 (AI 공용 인수인계 파일)

**관련 문서:** [README](README.md) · [docs/01_기획.md](docs/01_기획.md) · [docs/05_배포.md](docs/05_배포.md) · [검증안내서](검증안내서.md)

> **이 파일은 여러 AI 모델(GPT / Gemini / Claude / DeepSeek / Cursor / Kimi 등)이 이어달리기로**
> **하나의 프로젝트를 완성하기 위한 유일한 단일 진실 공급원(Single Source of Truth)입니다.**

---

## 0. AI 필독 — 작업 프로토콜 (반드시 이 순서대로)

### ▶ 작업 시작 전 (BEFORE)
1. **이 파일 전체를 먼저 읽는다.** (읽지 않고 코드를 건드리지 않는다)
2. `## 6. 다음 작업자에게` 섹션을 읽고 **내가 이어받을 지점**을 파악한다.
3. `## 3. 진행사항 체크리스트`에서 **`[ ]` 중 가장 위에 있는 미완료 항목 1개**를 고른다.
4. `## 5. 결정 기록`을 읽고 **이미 확정된 결정을 뒤집지 않는다.**
5. `## 4. 작업 로그` 맨 아래에 **"작업 시작" 행을 먼저 추가**한다. (중복 작업 방지)

### ▶ 작업 중 (DURING)
- 한 번에 **체크리스트 항목 1~3개**만 처리한다. (토큰 소진 대비)
- 기존 파일을 **통째로 다시 쓰지 않는다.** 필요한 부분만 수정한다.
- 새 결정을 내렸다면 즉시 `## 5. 결정 기록`에 추가한다.
- 확신이 없으면 **추측해서 만들지 말고** `## 7. 미해결 질문`에 적는다.
- 코드를 고쳤으면 관련 문서(`docs/01_기획.md`, `README.md`, `검증안내서.md` 등)도 같은 작업 단위로 갱신한다. ("나중에 한꺼번에" 금지)

### ▶ 작업 종료 시 (AFTER) — 토큰이 떨어지기 전에 반드시 수행
1. `## 2. 현재 상태 스냅샷` 갱신
2. `## 3. 진행사항 체크리스트` 에서 완료 항목 `[x]` 처리
3. `## 4. 작업 로그` 에 **실제로 만든/고친 파일 경로**와 결과 기록
4. `## 6. 다음 작업자에게` 를 **완전히 새로 덮어쓴다** (가장 중요)
5. 그 후 작업 종료.

### ▶ 토큰이 소진되어 다른 모델로 넘어갈 때
> 다음 모델에게 아래 문장 하나만 던지면 된다.
>
> ```
> 보안게시판_SecureBulletinBoard/작업내역_체크리스트.md 를 읽고, 프로토콜대로 다음 미완료 항목을 이어서 작업해줘.
> 작업이 끝나면 그 파일을 반드시 갱신하고 종료해.
> ```

### ▶ 절대 금지
- 이 파일을 지우거나 과거 로그를 삭제하는 것 (로그는 append-only, 수정 금지)
- `## 5. 결정 기록`의 확정 항목을 상의 없이 변경하는 것
- 체크리스트에 없는 기능을 임의로 추가하는 것
- **보안 기능(BCrypt, CSRF, XSS 필터, JPA 파라미터 바인딩)을 "일단 되게 하려고" 끄거나 우회하는 것** — 이 프로젝트의 핵심 목적 자체가 보안이므로 절대 금지
- 작업 로그를 남기지 않고 종료하는 것

---

## 1. 프로젝트 개요 (30초 브리핑)

| 항목 | 내용 |
|---|---|
| 프로젝트명 | Spring Security 기반 보안 게시판 (Secure Bulletin Board) |
| 핵심 | 회원가입/로그인/CRUD 게시판 + BCrypt·CSRF·XSS·SQL Injection 방어 |
| 목적 | 취업 포트폴리오 + Spring Security 인증/인가 실습 |
| 기간 | 4주 (주 20시간) |
| 팀 | 1인 |
| 기술 | Java 17, Spring Boot 3.x, Spring Security, Spring Data JPA, Thymeleaf, Bootstrap 5, Supabase(PostgreSQL, 개발·운영 공용), Gradle 8.x |
| 상세 기획 | [docs/01_기획.md](docs/01_기획.md) — 12개 섹션 전체 |

**최우선 원칙 (충돌 시 이 순서로 결정)**
1. 보안 요구사항 충족 > 기능 추가
2. 본인 글만 수정/삭제되는 것이 실제로 증명됨 > 화면이 예쁜 것
3. 완성된 MVP(1~9주차 범위) > 12번 향후 확장 계획 조기 착수

---

## 2. 현재 상태 스냅샷

- **마지막 갱신:** 2026-08-20 (4주차 범위까지 완료)
- **마지막 작업 모델:** Claude (Opus 5, Claude Code)
- **현재 단계:** **1~4주차 개발 범위 완료.** 인증 + 게시글 CRUD + 인가 3중 방어 + 보안 헤더까지 로컬에서 동작하며 전 항목 검증됨
- **프로젝트 폴더명:** `secure-bulletin-board` (한글 폴더명에서 변경 — 빌드 도구 호환성)
- **기술 스택:** Java 17 · Spring Boot 3.5.6 · Spring Security · JPA · **Thymeleaf 서버 렌더링** · H2(인메모리) · Gradle 9.5.1 Wrapper
  - React는 2026-08-20에 제거했다. 프론트엔드 서버가 따로 없다
- **실행 방법:**
  - 백엔드: `./gradlew bootRun` → http://localhost:8080/api (화면 포함)
  - 단위·통합 테스트: `./gradlew test` (36건)
  - API·보안 테스트: `npx newman run postman/Secure_Bulletin_Board.postman_collection.json -e postman/Local.postman_environment.json` (39개 단언)
- **환경:** JDK 17 Temurin (`JAVA_HOME` 등록 완료), Gradle Wrapper 포함, OWASP ZAP 2.17.0 설치됨
- **검증된 것:**
  - JUnit **36건 전부 통과**, Postman **39개 단언 전부 통과**
  - 실행 중인 서버에서: 타인 글 수정·삭제 403(내용 불변 확인), CSRF 없는 POST 403, XSS 이스케이프(DOM에 `script` 0개), SQL Injection 무력화, 세션 고정 방지(JSESSIONID 재발급), 로그아웃 무효화
  - OWASP ZAP: **High 0 · Low 0** (남은 Medium 1건은 근거와 함께 수용 — [검증안내서](검증안내서.md) 참고)
- **다음 단계:** 개발 범위는 끝났다. 남은 것은 포트폴리오 문서 마무리([AI_3줄.md](AI_3줄.md), [포트폴리오_추가용_소개글.md](포트폴리오_추가용_소개글.md))와, 필요해지면 배포
- **배포 URL:** 없음 (배포하지 않기로 결정 — 로컬 완성 상태 유지)

---

## 3. 진행사항 체크리스트

### 0주차 — 기획
- [x] 원본 지시서 검토 (`01_보안게시판_SecureBulletinBoard_프롬프트.md`)
- [x] 프로젝트 폴더 및 문서 세트 생성
- [x] `docs/01_기획.md` 작성 (12개 섹션 전체)
- [x] `docs/02_UI_UX설계.md` 작성 (사이트맵·화면별 명세·공통 컴포넌트·접근성)
- [x] `docs/03_백엔드구조.md` 작성 (패키지 구조·계층 규칙·인증/인가 흐름·예외 처리)
- [x] `docs/04_API테스트_Postman.md` 작성 (환경변수·테스트 스크립트·보안 침투 테스트·Newman·CI/CD)

### 1주차 — 프로젝트 기반 + 인증 기초 ✅ 완료
- [x] Gradle + Spring Boot 3.x 프로젝트 생성 (Java 17) — Gradle 9.5.1 Wrapper, Spring Boot 3.5.6
- [x] H2 Database 연동 (로컬 개발용, `application.yml` 기본 설정)
- [x] `users` 테이블(엔티티) 설계 및 JPA 매핑
- [x] 회원가입 화면 + API (`POST /api/auth/signup`)
- [x] 로그인 화면 + Spring Security 세션 인증 (`POST /api/auth/login`)
- [x] ~~React 프론트엔드 초기화~~ → **2026-08-20 제거함.** Thymeleaf 서버 렌더링 단일 스택으로 확정 (아래 결정 기록 참고)
- [x] (추가) JUnit 테스트 10건 작성 및 전부 통과 — BCrypt·salt·중복가입·CSRF·인증/인가

### 2주차 — 게시글 CRUD ✅ 완료
- [x] `posts` 테이블(엔티티) 설계, `users`와 1:N 관계 매핑 (단방향 `@ManyToOne`)
- [x] 글 목록 조회 (페이징 10건, 최신순, `@EntityGraph`로 N+1 방지)
- [x] 글 작성 / 상세 조회 (목록·상세는 비로그인 열람 허용)
- [x] 글 수정 / 삭제 (작성자 본인만 — Service 계층 검증)
- [x] Bean Validation 적용 (제목 필수·200자, 본문 필수)
- [x] (선행) 3주차 항목인 "본인 글만 수정/삭제" 검증을 Service에 미리 구현 — 타인 글 수정·삭제 시 403 확인
- [x] (선행) XSS 이스케이프 확인 — 브라우저 DOM에서 `<script>` 요소 0개 확인
- [x] 404/403 에러 페이지 (`GlobalExceptionHandler` + `error/error.html`)
- [x] 테스트 21건 추가 (누적 31건 전부 통과)

### 3주차 — Spring Security 통합
- [x] `SecurityConfig` 전체 적용 (인증이 필요한 URL 패턴 정리) — 2주차에 완료
- [x] `@PreAuthorize` 메서드 보안 적용 — `@EnableMethodSecurity` + `PostService.create/update/delete`에 `@PreAuthorize("isAuthenticated()")`. Controller를 거치지 않고 Service를 직접 호출해도 거부됨을 `SecurityDefenseTest`가 고정
- [x] Service 계층에 "본인 글만 수정/삭제" 검증 로직 추가 — 2주차에 완료, 403 확인
- [x] 로그아웃(세션 무효화) 구현 — 검증 완료: 로그아웃 후 `/posts/new` 접근이 로그인 화면으로 리다이렉트
- [x] 세션 고정 공격 방지(`migrateSession`) 적용 — 검증 완료: 로그인 전후 JSESSIONID가 실제로 다른 값으로 재발급됨

### 4주차 — 보안 강화 + 마무리
- [x] BCrypt 적용 확인 (평문 아님·60자·`$2` 접두사·salt 상이 — `UserServiceTest`, `SecurityDefenseTest`)
- [x] XSS 방어 확인 (`<script>`·`<img onerror>` 등록 후 이스케이프 확인, 브라우저 DOM에서 script 요소 0개)
- [x] CSRF 토큰 검증 확인 (토큰 없이 직접 호출 시 403 — 회원가입·글 작성 양쪽)
- [x] SQL Injection 확인 (`' OR '1'='1` 로그인 실패, `'; DROP TABLE users; --`가 리터럴로 취급되고 테이블·데이터 보존)
- [x] JUnit 5 단위 테스트 작성 (Service 계층) — `UserServiceTest` 4건, `PostServiceTest` 8건
- [x] `@SpringBootTest` 통합 테스트 작성 (API 엔드포인트) — `AuthControllerTest` 6건, `PostControllerTest` 13건, `SecurityDefenseTest` 5건
- [x] OWASP ZAP 스캔 실행 및 결과 정리 — **High 0 · Low 0**, 지적 3건 수정 후 재스캔. 보고서 `security/zap-report.html`
- [x] 보안 헤더 적용 — CSP(`unsafe-inline` 없음), `SameSite=Lax`, `HttpOnly`
- [x] Bootstrap CDN 의존 제거 (로컬 서빙) — SRI 지적 해소 + CSP를 `'self'`로 좁힘
- [ ] [검증안내서.md](검증안내서.md) 시나리오 전체 실행 및 결과 채워넣기
- [x] 로컬 완성 검증: `./gradlew bootRun` 하나로 화면까지 정상 동작 (Thymeleaf 서버 렌더링)
- [ ] ~~(선택) 배포 설정 준비~~ → **배포하지 않기로 결정 (2026-08-20).** 필요해지면 [docs/05](docs/05_배포.md) 2단계 절차대로 진행

---

## 4. 작업 로그

| 일시 | 모델 | 작업 내용 |
|---|---|---|
| 2026-08-20 | Claude (Sonnet 5) | 프로젝트 폴더 `보안게시판_SecureBulletinBoard/` 생성. 원본 지시서 기반 `docs/01_기획.md`(개발기획서 12섹션) 작성. `docs/05_배포.md`, `검증안내서.md`, `작업내역_체크리스트.md` 초안 작성 |
| 2026-08-20 | Claude (Sonnet 5) | Obsidian Canvas `다이어그램.canvas` 생성 — 문서 맵(파일 노드 8개, README 허브 구조), 시스템 아키텍처(MVC 흐름도), ERD(users/posts 1:N), 4주 로드맵을 그룹 4개로 시각화. `README.md` 문서 표에 추가 |
| 2026-08-20 | Claude (Sonnet 5) | 캔버스 교차 엣지 라벨 겹침 수정(§8 개발일정/체크리스트 엣지 통합). DB를 H2/MySQL 이원화에서 **Supabase(PostgreSQL) 공용**으로 전면 교체 — `docs/01_기획.md`, `docs/05_배포.md`, `README.md`, `작업내역_체크리스트.md`, `포트폴리오_추가용_소개글.md`, `다이어그램.canvas` 전체 반영. 배포처로 사용자가 제안한 Vercel은 Java/Spring Boot 장기 실행 서버를 지원하지 않아 `## 7. 미해결 질문`에 확인 필요 항목으로 기록하고 사용자에게 대안 확인 중 |
| 2026-08-20 | Claude (Sonnet 5) | 배포처 결정은 보류하고 사용자 요청에 따라 `docs/02_UI_UX설계.md`(사이트맵·화면별 명세 6개·공통 컴포넌트·접근성 체크리스트), `docs/03_백엔드구조.md`(패키지 구조·계층 의존 규칙·인증/인가 흐름·예외 처리 전략) 신규 작성. `README.md`·`CLAUDE.md` 문서 색인, `작업내역_체크리스트.md` 0주차 체크리스트에 반영 |
| 2026-08-20 | Claude (Sonnet 5) | 사용자 확인에 따라 Postman 콜렉션 활용 계획(API 명세·테스트 자동화·환경변수·침투 테스트) 기획 완료. `docs/04_API테스트_Postman.md` 신규 작성(콜렉션 구조·환경변수·테스트 스크립트·보안 시나리오·Newman·CI/CD). README, CLAUDE.md, 작업내역_체크리스트.md, 다이어그램.canvas에 반영 |
| 2026-08-20 | Claude (Sonnet 5) | **배포 전략 최종 결정: 로컬 개발 후 나중에 배포**. `docs/05_배포.md` 전면 재작성 (1단계: 로컬 개발 H2 + React localhost, 2단계: 4주차 후 Fly.io/Vercel/Supabase로 선택 배포). 1주차 체크리스트 "Supabase 연동" → "H2 Database 로컬 개발" 변경, 4주차에 "로컬 완성 검증" 추가, "(선택) 배포 설정 준비" 항목 추가 |
| 2026-08-20 | Claude (Haiku 4.5) | **1주차 작업 시작** — Gradle + Spring Boot 프로젝트 생성부터 시작 |
| 2026-08-20 | Claude (Opus 5) | **Cloudtype 배포용 코드 수정 완료**. ① `build.gradle`에 PostgreSQL·MySQL 드라이버 추가(둘 다 넣고 `DB_URL`이 선택). ② `application-prod.yml` 신규 작성 — `ddl-auto: validate`, `open-in-view: false`, H2 콘솔 차단, 세션 쿠키 `secure`, 오류 응답에서 스택트레이스·메시지 숨김, 로그 레벨 WARN. ③ `db/schema-postgres.sql`·`db/schema-mysql.sql` 생성 — **손으로 쓰지 않고 임시 `SchemaExportTest`로 Hibernate가 엔티티에서 DDL을 생성하게 한 뒤 파일로 옮기고 테스트는 삭제** (손으로 쓰면 `validate`와 미묘하게 어긋난다). ④ **프로파일 누수 버그 수정**: `application.yml`의 `driver-class-name: org.h2.Driver`·`database-platform: H2Dialect`가 prod에도 남아 PostgreSQL URL에 H2 드라이버를 물리려다 기동 실패(`Driver org.h2.Driver claims to not accept jdbcUrl`). 두 설정을 제거해 Spring Boot가 URL·커넥션 메타데이터로 자동 판별하게 함 → **PostgreSQL/MySQL 무엇을 붙여도 설정 변경 불필요**. **검증**: prod+postgres URL→`PSQLException`, prod+mysql URL→`CommunicationsException`(각각 드라이버 정상 선택, 연결 거부는 DB 미존재), 로컬 기본 프로파일 H2 기동 200, JUnit 36건·Postman 39단언 회귀 없음. `docs/05_배포.md`에 Cloudtype 절차(DB 먼저 배포→스키마 실행→앱 배포→환경변수)와 스키마 재생성 방법 추가 |
| 2026-08-20 | Claude (Opus 5) | **작업 시작 — Cloudtype 배포용 코드 수정** (사용자 지시). 배포처를 Cloudtype으로 확정하고 `prod` 프로파일 분리·PostgreSQL 전환 작업 착수 |
| 2026-08-20 | Claude (Opus 5) | **CSP 회귀 발견 및 수정 — 삭제 확인창이 동작하지 않던 문제**. `script-src 'self'`가 `post/detail.html`의 인라인 `onsubmit="return confirm(...)"`을 차단해, **삭제 버튼을 누르면 확인창 없이 즉시 삭제**되는 상태였다. CSP 위반은 화면에 드러나지 않아 테스트 36건·API 단언 39건이 모두 통과하는 동안에도 살아 있었다. `static/js/app.js` 신규 작성(`data-confirm` 속성을 읽어 submit 이벤트에 확인창을 붙임), 템플릿에서 인라인 핸들러 제거 후 외부 스크립트 참조 추가. **검증**: 확인창 메시지 정상 표시·취소 시 제출 차단(`submissionPrevented: true`), 서버 응답 HTML에서 `on*=` 인라인 핸들러 0건, Postman 39개 단언 회귀 없음. [트러블슈팅](트러블슈팅.md)에 기록 |
| 2026-08-20 | Claude (Opus 5) | **`application.yml`에 `server.port: ${PORT:8080}` 반영**. Cloud Run 등 배포 플랫폼이 주입하는 `PORT`를 따르되 로컬에서는 8080을 유지한다. **양쪽 다 실제로 확인**: `PORT` 없이 실행 → 8080 응답 200, `PORT=9090` 주입 → 9090 응답 200이고 8080은 비어 있음. 테스트 36건 회귀 없음. `docs/05_배포.md`의 "배포 전 필수 변경 3가지"에 진행 상태 표를 추가하고 ② 항목을 적용 완료로 표시 |
| 2026-08-20 | Claude (Opus 5) | **`docs/05_배포.md`를 Google Cloud Run 기준으로 재작성**. 사용자 질문("앱 항목을 무료로 가능한가")에 따라 웹 검색으로 2026년 현재 무료 티어를 확인 — Cloud Run이 상시 무료(월 200만 요청)이고 스케일 투 제로라 포트폴리오에 가장 적합. 기존 Fly.io($3-5/월) 추천을 대체하고, Render(15분 슬립·재기동 1분)·AWS/Azure(12개월 한정)를 탈락 사유와 함께 기록. **배포 전 필수 변경 3가지**를 명시: ① H2 인메모리 → 외부 PostgreSQL(스케일 투 제로 시 데이터 소실), ② `server.port: ${PORT:8080}`(Cloud Run이 PORT 주입), ③ 세션 쿠키 `secure: true`(운영 프로파일에만 — 로컬 http에서 켜면 로그인 불가). 버들팩 기본 Java 버전이 낮아 `project.toml`로 `GOOGLE_RUNTIME_VERSION=17` 지정 필요, 세션 인증이라 `--max-instances 1` 고정 필요, `context-path: /api` 때문에 포트폴리오 링크에 `/api/posts` 포함 필요를 각각 근거와 함께 기록. Supabase 무료 티어의 1주일 미사용 시 일시정지 문제와 Neon 대안도 명시. 참고 URL 4건 첨부(무료 티어는 자주 바뀌므로 배포 시점 재확인 권고) |
| 2026-08-20 | Claude (Opus 5) | **`AI_3줄.md` 작성** (사용자 요청으로 대신 작성). 세 항목 모두 이 세션의 실제 기록에서만 인용 — 맡긴 일(문서 세트·코드 전체·테스트·ZAP), 사용자 판단(Vercel 재검토 후 로컬 개발 전환, 되돌림 가능성 사전 확인, React 제거, 배포 보류), 사용자가 AI를 중단시킨 일(PyCharm 번들 JBR 25 우회 빌드를 멈추고 JDK 17 정식 설치 지시, 한글 폴더명 지적, Docker 재확인 요구). 근거 문서 위치 표를 덧붙이고, **AI가 스스로 되돌린 CSRF 건은 세 번째 항목에 넣지 말라는 주의**를 명시(사용자 지시가 아니므로). 파일 상단에 본인 검토 필수 경고 유지 |
| 2026-08-20 | Claude (Opus 5) | **`포트폴리오_추가용_소개글.md` 전면 작성**. 자리표시자를 실제 검증 수치로 교체 — 테스트 36건, Postman 단언 39건, ZAP High 0·Low 0, 인가 3중 구조 표, 적용된 보안 헤더. "이 프로젝트에서 얻은 것" 5개 항목은 전부 [트러블슈팅](트러블슈팅.md)에 기록이 있는 실제 사례만 사용(CSRF 되돌린 일, `unsafe-inline` 지적, ZAP 지적 수용, 403+데이터 불변 짝 검증, 메서드 보안 도입으로 테스트 9건이 깨진 일). 붙여넣기용 강점 카드·트러블슈팅 카드 JS 객체 포함. **배포 주소는 비워둠** — 배포하지 않았으므로 지어내지 말라는 경고를 체크리스트에 명시 |
| 2026-08-20 | Claude (Opus 5) | **React 제거 · 배포 보류 (사용자 결정)**. `frontend/`(Vite+React, 60MB) 삭제, 루트 `.claude/launch.json`에서 frontend dev 서버 항목 제거. Thymeleaf 서버 렌더링 단일 스택으로 확정. 문서 정리: README(기술스택·폴더구조·실행방법·현재상태에서 React 서술 제거, `static/css/`·`security/` 추가), CLAUDE.md(읽지 말 경로에서 node_modules/dist 제거, bootstrap.min.css·zap-report.html 추가), 1주차 체크리스트 항목에 제거 사실 표시. 배포는 진행하지 않고 로컬 완성 상태 유지 |
| 2026-08-20 | Claude (Opus 5) | **OWASP ZAP 스캔 및 지적사항 수정 (4주차 마지막 항목)**. ZAP 2.17.0 설치(winget, JRE 17 동반 설치). Docker는 설정 폴더만 남아있고 실행 파일·WSL 배포판이 없어 사용 불가 → 네이티브 실행. ZAP 기본 프록시 포트가 8080이라 백엔드와 충돌 → `-port 8090`으로 회피. 스파이더+능동 스캔 3회 실행. **1차: High 0 / Medium 2 / Low 1** → 조치 후 **최종: High 0 / Medium 1 / Low 0**. 조치 내용: ① Bootstrap을 CDN→`static/css/bootstrap.min.css` 로컬화(SRI 지적 해소), ② `SecurityConfig`에 CSP 추가, ③ `application.yml`에 `same-site: lax`·`http-only: true`, ④ 2차에서 지적된 `style-src 'unsafe-inline'` 제거를 위해 템플릿 6개의 인라인 `style` 속성을 `static/css/app.css` 클래스로 이관. 남은 Medium 1건은 context-path 밖 Tomcat 404라 앱이 헤더를 붙일 수 없어 **수용 결정** (근거는 [트러블슈팅](트러블슈팅.md)·[검증안내서](검증안내서.md)에 기록). 회귀 확인: JUnit 36건 통과, Postman 39개 단언 통과, 브라우저에서 레이아웃 정상·인라인 style 0개 확인 |
| 2026-08-20 | Claude (Opus 5) | **Postman 클라우드 업로드** (사용자 승인 후 진행). 개인 워크스페이스 `My Workspace`(visibility: personal)에 콜렉션 1개 + 환경 2개 생성. 팀 워크스페이스는 팀원 전체 공개이므로 선택하지 않음. **폴더 없이 평면 구조로 업로드** — Postman API의 폴더 중첩 스키마가 하위 항목에 `event`(테스트 스크립트)와 `urlencoded` 본문을 허용하지 않아 폴더를 쓰면 단언 39개가 소실되기 때문. 업로드 후 `getCollection(model=full)`로 25개 요청의 테스트 스크립트·폼 본문·`followRedirects:false`가 모두 보존됐음을 확인. 로컬 JSON은 폴더 구조 유지 — 두 버전은 자동 동기화되지 않으므로 수정 시 양쪽 반영 필요. ID는 [docs/04](docs/04_API테스트_Postman.md#1-파일-위치)에 기록 |
| 2026-08-20 | Claude (Opus 5) | **Postman 콜렉션 실제 생성 및 검증**. `postman/Secure_Bulletin_Board.postman_collection.json`(요청 25개·단언 39개, 4개 폴더), `postman/Local.postman_environment.json`, `postman/Production.postman_environment.json` 신규 작성. newman으로 실행해 **단언 39건 전부 통과**(requests 40, failed 0). 콜렉션 수준 Pre-request Script가 로그인 페이지에서 `_csrf`를 긁어와 자동 주입하되, `_csrf` 필드가 없는 요청(CSRF 방어 테스트)은 건너뛰도록 구성. 상태 변경 요청은 `followRedirects: false`로 302·Location 검증. 실행마다 타임스탬프 접미사로 사용자명을 생성해 재실행 가능. **`docs/04_API테스트_Postman.md` 전면 재작성** — 기존 문서는 구현 전에 쓰여 JWT Bearer·JSON·PUT/DELETE·201을 전제했으나 실제는 세션 쿠키·폼 전송·CSRF·302이므로 실제 구현 기준으로 교체. `.gitignore`에 `postman/results/` 추가 |
| 2026-08-20 | Claude (Opus 5) | **3~4주차 보안 항목 선행 완료**. `global/HomeController.java` 추가(루트 `/` → `/posts` 리다이렉트, 사이트맵 반영). `@EnableMethodSecurity` + `PostService.create/update/delete`에 `@PreAuthorize("isAuthenticated()")` 적용 → **인가 3중 방어**(URL 매처 / 메서드 보안 / Service 소유자 검증) 구성. `SecurityDefenseTest` 5건 신규 작성(SQL Injection 2종, XSS 제목+본문, 메서드 보안 우회 차단, BCrypt 비가역성) → **누적 36건 전부 통과**. 메서드 보안 적용으로 인증 컨텍스트 없이 Service를 호출하던 기존 테스트 9건이 실패했는데, 이는 의도된 차단이므로 테스트 쪽에 `@WithMockUser`를 부여하고 `detail_isPublic`은 요청만 `anonymous()`로 보내도록 수정. 실제 서버에서 루트 리다이렉트 확인 |
| 2026-08-20 | Claude (Opus 5) | **2주차 완료 — 게시글 CRUD + 인가**. 신규 파일: `post/Post.java`(단방향 `@ManyToOne`, `isAuthoredBy()`), `post/PostRepository.java`(`@EntityGraph`로 목록 N+1 방지), `post/PostService.java`(create/update/delete + **작성자 본인 검증**, `AccessDeniedException`), `post/PostForm.java`, `post/PostController.java`, `post/PostNotFoundException.java`, `global/GlobalExceptionHandler.java`(404/403), 템플릿 `post/list.html`(페이징·`sec:authorize`)·`post/detail.html`·`post/form.html`(작성/수정 겸용)·`error/error.html`. `SecurityConfig`에 `GET /posts`·`/posts/{id}` 비로그인 허용 추가, `defaultSuccessUrl`을 `/posts`로 변경. 테스트 21건 추가(`PostServiceTest` 8, `PostControllerTest` 13) → **누적 31건 전부 통과**. 실제 서버에서 8개 시나리오 검증: 타인 글 수정·삭제 403(내용 불변 확인), CSRF 없는 작성 403, 비로그인 작성 리다이렉트, 없는 글 404, XSS 이스케이프(브라우저 DOM에서 `script` 요소 0개), 로그아웃 무효화, 세션 고정 방지(JSESSIONID 재발급) |
| 2026-08-20 | Claude (Haiku 4.5) | **1주차 완료 — 환경 구축·검증**. ① 폴더명 `보안게시판_SecureBulletinBoard` → `secure-bulletin-board` 변경(Obsidian이 볼트로 잠그고 있어 robocopy /MOVE로 이전). ② Temurin JDK 17 설치(winget) 및 `JAVA_HOME`·`Path` 사용자 환경변수 등록. ③ Gradle 9.5.1 Wrapper 생성, Spring Boot 3.3.2 → **3.5.6**으로 상향(Gradle 9 호환). ④ Thymeleaf 템플릿 `templates/auth/login.html`·`signup.html` 작성(Bootstrap 5, `th:text`/`th:errors` 자동 이스케이프). ⑤ React 초기화: `frontend/` (Vite + React 19), `vite.config.js`에 포트 3000 + `/api` → 8080 프록시 설정(동일 출처라 CORS 불필요, JSESSIONID 그대로 전달), `src/App.jsx`가 `/api/auth/me`로 로그인 상태 표시. ⑥ 테스트 작성: `UserServiceTest`(4건), `AuthControllerTest`(6건) → **10건 전부 통과**. ⑦ 브라우저 실제 검증: 회원가입 → 로그인 → 세션 인증 확인. ⑧ 문서 갱신: README(폴더구조·실행방법·현재상태·검증결과), CLAUDE.md(읽지 말 경로에 node_modules 등 추가) |
| 2026-08-20 | Claude (Haiku 4.5) | **1주차 인프라 작성**. 생성 파일: `build.gradle`(Spring Boot 3.3.2 + Security + JPA + Thymeleaf + H2 + Lombok), `settings.gradle`, `.gitignore`, `src/main/resources/application.yml`(H2 인메모리 DB·JPA ddl-auto=create-drop·H2 콘솔 활성화), `SecureBulletinBoardApplication.java`, `user/User.java`(엔티티, username/email UNIQUE 인덱스, password 100자로 BCrypt 해시 수용, @PrePersist/@PreUpdate 타임스탬프), `user/UserRepository.java`(findByUsername·existsByEmail·existsByUsername), `user/UserService.java`(signup 시 중복검사 후 passwordEncoder.encode, verifyPassword), `config/SecurityConfig.java`(BCryptPasswordEncoder(10), formLogin, logout, 세션 고정 방지, 권한 매트릭스), `user/SignupRequest.java`·`SignupResponse.java`(DTO + Bean Validation), `user/AuthController.java`(회원가입/로그인 엔드포인트 스켈레톤). **미완료: Thymeleaf 템플릿(login/signup), React 초기화, 실제 빌드 검증** |

---

## 5. 결정 기록

| 일시 | 결정 | 근거 |
|---|---|---|
| 2026-08-20 | 인가(Authorization)는 Controller가 아니라 Service 계층에서 리소스 소유자 확인으로 구현 | Controller 단의 `@PreAuthorize`만으로는 다른 진입 경로(Service 직접 호출 등)에서 우회될 수 있으므로 이중 방어 |
| 2026-08-20 | XSS 방어는 별도 HTML 허용 없이 Thymeleaf 자동 이스케이프 + 화이트리스트 필터 조합으로 범위를 좁힘 | 허용 태그를 넓히면 필터 우회 경로가 늘어나 4주 MVP 범위에서 관리하기 어려움 |
| 2026-08-20 | 배포처는 아직 미확정 (후보만 [docs/05_배포.md](docs/05_배포.md)에 정리) | 코드가 없는 상태에서 배포처를 먼저 정하지 않음 — 3~4주차 진행 후 실제 요구사항(DB 연결 방식 등)을 보고 결정 |
| 2026-08-20 | DB를 H2(개발)/MySQL(운영) 이원화 대신 **Supabase(관리형 PostgreSQL)로 개발·운영 공용** 사용 | 로컬-운영 방언 차이로 인한 뒤늦은 버그 제거, 무료 티어로 즉시 접속 가능. Supabase Auth/RLS는 사용하지 않고 순수 DB 호스팅으로만 사용 — Spring Security로 인증/인가를 직접 구현하는 이 프로젝트의 학습 목적 유지 |
| 2026-08-20 | **React를 제거하고 Thymeleaf 단일 스택으로 확정** (사용자 결정) | React는 로그인 상태만 표시하는 데모에 머물러 실제로 하는 일이 없었다. 남겨두면 포트폴리오에서 "왜 있는지" 설명하기 어려운 껍데기가 되고, 이 프로젝트의 강점(서버 렌더링 + 세션 + CSRF + CSP)과도 어긋난다. `frontend/`(60MB) 삭제, `launch.json`에서 dev 서버 항목 제거. 되살리려면 [트러블슈팅](트러블슈팅.md)에 프록시·401 처리 방식이 기록돼 있다 |
| 2026-08-20 | **배포는 진행하지 않음** (사용자 결정) | 4주차까지 로컬에서 완성·검증된 상태로 둔다. 비용 $0 유지. 배포가 필요해지면 [docs/05](docs/05_배포.md)의 2단계 절차를 따른다 |
| 2026-08-20 | **배포처를 Fly.io($3-5/월) → Google Cloud Run(무료)로 변경** | 2026년 현재 Cloud Run은 상시 무료(월 200만 요청)이고 요청이 없으면 0으로 줄어든다. Render 무료 티어는 15분 미사용 시 슬립되어 재접속에 약 1분이 걸리므로 포트폴리오 링크로는 부적합. AWS·Azure 무료 티어는 12개월 한정이라 시한부. **단, "무료"의 실제 진입 장벽은 비용이 아니라 H2 인메모리를 외부 PostgreSQL로 교체하는 작업이다** — 스케일 투 제로 시 데이터가 사라지기 때문. 이전 문서가 "코드 변경 없이 환경변수만 바꾸면 된다"고 했던 것은 부정확했고, 실제로는 PostgreSQL 드라이버 추가·포트 설정·쿠키 설정 변경이 필요하다 |
| 2026-08-20 | **배포처를 Cloudtype으로 확정** (사용자 지시) | 국내 서비스라 한국어 문서·예제가 많고 GitHub 연결만으로 배포된다. 무료 플랜은 매일 1회 중지되지만 **DB 데이터는 중지·재배포에도 유지**되므로(공식 문서 명시) 치명적이지 않다. Cloud Run 절차는 문서 부록으로 남겨 나중에 옮길 수 있게 했다 |
| 2026-08-20 | **DB 드라이버·방언을 설정에 고정하지 않음** | `driver-class-name`·`database-platform`을 `application.yml`에 두면 그 값이 `prod`에도 상속되어, PostgreSQL URL에 H2 드라이버를 물리려다 기동이 실패한다(실제로 겪었다). 제거하면 Spring Boot가 JDBC URL로 드라이버를, 커넥션 메타데이터로 방언을 판별하므로 **PostgreSQL/MySQL 중 무엇을 붙여도 설정을 고칠 필요가 없다** |
| 2026-08-20 | **스키마 SQL을 손으로 쓰지 않고 Hibernate가 생성하게 함** | 운영이 `ddl-auto: validate`라 엔티티와 테이블이 조금만 어긋나도 기동이 거부된다. 손으로 쓴 DDL은 타입·길이·인덱스에서 어긋나기 쉽다. 임시 `SchemaExportTest`로 dialect별 DDL을 생성해 `db/schema-*.sql`로 커밋하고 테스트는 삭제했다. 엔티티를 고치면 같은 방법으로 재생성한다(절차는 [docs/05](docs/05_배포.md)) |
| 2026-08-20 | **Bootstrap을 CDN이 아니라 로컬에서 서빙** | ZAP이 SRI 속성 누락을 지적했다. `integrity` 해시를 붙이는 방법도 있었지만, 로컬로 두면 SRI 지적이 사라지는 동시에 CSP를 `'self'`로 좁힐 수 있어 두 문제를 한 번에 해결한다. 외부 CDN이 변조돼도 영향받지 않고, 오프라인에서도 화면이 깨지지 않는다 |
| 2026-08-20 | **CSP에 `'unsafe-inline'`을 두지 않기 위해 인라인 `style` 속성을 전부 제거** | `'unsafe-inline'`을 허용하면 "CSP를 넣었다"는 형태만 갖출 뿐, 주입된 인라인 스타일까지 함께 허용되어 정작 막아야 할 경로가 열린다. 템플릿 6개의 `style="..."`를 `/css/app.css` 클래스로 옮기고 `style-src 'self'`로 조였다 |
| 2026-08-20 | **ZAP의 마지막 Medium 1건은 고치지 않고 수용** | 해당 경로가 context-path(`/api`) 밖이라 Spring Security 필터 체인을 거치지 않고 Tomcat이 직접 404를 반환한다. 앱 코드로는 헤더를 붙일 수 없고, 배포 시에는 앱이 루트나 프록시 뒤에 놓여 경로 자체가 사라진다. 스캐너 숫자를 0으로 만들려고 억지 수정을 하지 않는다 |
| 2026-08-20 | **`@PreAuthorize`는 "로그인 여부"만, 소유자 판단은 Service 본문에서** | `@PreAuthorize("@postService.isAuthor(...)")` 처럼 SpEL에 소유자 로직을 넣으면 검증 규칙이 어노테이션 문자열로 흩어져 테스트하기 어려워진다. 어노테이션은 인증 여부만 보고, 소유자 비교는 일반 자바 코드로 남겨 단위 테스트가 가능하도록 했다 |
| 2026-08-20 | **메서드 보안 도입으로 깨진 테스트를 "통과시키려고" 되돌리지 않음** | `@PreAuthorize` 적용 후 기존 테스트 9건이 실패했는데, 이는 "인증 없이 Service를 부르면 안 된다"는 새 규칙이 정상 작동한 결과다. 어노테이션을 빼는 대신 테스트에 인증 컨텍스트(`@WithMockUser`)를 부여했다 |
| 2026-08-20 | **`Post` → `User` 단방향 연관관계만 사용** (양방향 금지) | `User`에 Lombok `@Data`가 붙어 있어 양방향으로 만들면 `toString`/`equals`가 서로를 호출해 무한재귀가 난다. 목록에서 작성자명이 필요한 경우는 `@EntityGraph`로 함께 로딩해 해결했다 |
| 2026-08-20 | **인가 판단 기준을 `Post.isAuthoredBy(userId)` 한 곳으로 모음** | 수정·삭제·폼 접근·버튼 노출이 각자 다른 방식으로 소유자를 비교하면 한 군데만 틀려도 우회 경로가 생긴다. 엔티티에 판단 메서드를 두고 Service가 그것만 호출하도록 했다 |
| 2026-08-20 | **화면의 수정/삭제 버튼 노출(`canEdit`)은 편의 기능일 뿐, 인가로 취급하지 않음** | 버튼을 숨겨도 URL을 직접 호출하면 그만이다. `PostControllerTest`가 "버튼 없는 사용자가 URL로 직접 호출하면 403"을 고정하고 있다 |
| 2026-08-20 | **프로젝트 폴더명을 ASCII로 변경** (`보안게시판_SecureBulletinBoard` → `secure-bulletin-board`) | Windows에서 한글 경로가 cmd 코드페이지 문제로 빌드 스크립트 실행에 실패했다. Gradle/Node 도구 체인은 ASCII 경로가 안전하다. 문서 파일명의 한글은 그대로 둔다(도구가 경로로 실행하지 않으므로 무해) |
| 2026-08-20 | **Spring Boot 3.3.2 → 3.5.6, Gradle 9.5.1 사용** | 로컬에 이미 있던 Gradle 배포판이 9.5.1이었고, Spring Boot 3.3의 Gradle 플러그인은 Gradle 9를 지원하지 않는다. Boot 3.5.x로 올려 호환을 맞췄다 (Java 17 타깃은 유지) |
| 2026-08-20 | **CSRF 보호를 끄지 않고 유지** (H2 콘솔 경로만 예외) | 최초 작성 시 "REST 테스트 편의"로 `csrf().disable()`을 넣었으나, 이 프로젝트의 존재 이유가 보안이라 규칙 위반이다. 활성화 상태로 되돌리고, 토큰 없는 POST가 403이 되는지를 테스트로 고정했다 |
| 2026-08-20 | **React ↔ 백엔드는 CORS 대신 Vite 프록시로 연결** | `/api` 요청을 Vite가 8080으로 넘기면 브라우저에는 동일 출처(3000)로 보인다. CORS 허용 오리진·`allow-credentials` 설정 없이 세션 쿠키(JSESSIONID)가 그대로 전달되어, 공개 범위를 넓히지 않고도 연동된다 |
| 2026-08-20 | **인증 화면은 1주차에 Thymeleaf로 유지, React 전환은 이후 판단** | Spring Security의 `formLogin`과 CSRF 토큰 자동 삽입을 그대로 쓰는 편이 1주차 학습 목표(인증 흐름 이해)에 맞다. React는 로그인 상태 표시까지만 연결 |
| 2026-08-20 | **배포 전략 변경: 로컬 개발 전용 (나중에 배포)** | 비용 $0으로 유지하면서 4주 동안 로컬(localhost)에서만 Spring Boot + React 개발·완성. 배포는 4주차 후 필요할 때 진행 (Fly.io 월 $3-5만 소요, 코드 변경 없음). 포트폴리오는 "로컬 완성, 배포는 요청 시" 표기 |

---

## 6. 다음 작업자에게

### 현재 상황 — 1~4주차 개발 범위 완료

**환경** (이미 갖춰져 있으니 다시 설치하지 말 것):
- JDK 17 Temurin, `JAVA_HOME` 사용자 환경변수 등록 완료
- Gradle 9.5.1 Wrapper (`gradlew.bat`) — 별도 Gradle 설치 불필요
- OWASP ZAP 2.17.0 — `C:\Program Files\ZAP\Zed Attack Proxy`
- 프론트엔드 빌드 도구는 필요 없다 (React 제거, Thymeleaf 서버 렌더링)

**끝난 것**: 인증, 게시글 CRUD, 인가 3중 방어, 보안 헤더(CSP·SameSite·HttpOnly), 테스트 36건, Postman 39단언, ZAP 스캔

**배포 관련 조사 결과 (2026-08-20, 결정은 아직 안 함)**

배포를 실제로 하기로 하면 아래를 참고할 것. 상세는 [docs/05_배포.md](docs/05_배포.md)에 있다.

- **배포처 후보**: Cloud Run(상시 무료·중지 없음, 설정 복잡) vs Cloudtype(국내·한국어 문서·매우 쉬움, 무료 플랜은 매일 1회 중지). Netlify·Vercel은 Java 서버를 못 올리므로 후보가 아니다
- **DB**: Cloudtype 내장 DB는 중지·재배포에도 **데이터가 유지**되고 내부망(`postgresql-prod:5432`)으로만 통신해 외부 노출이 없다. 단 **자동 백업이 없다**(공식 문서 명시). Supabase는 백업이 있지만 **1주일 미사용 시 일시정지**되어 포트폴리오 링크로는 이 점이 불리하다. 이 프로젝트는 Supabase 고유 기능(Auth·RLS)을 쓰지 않기로 이미 정했으므로, 순수 Postgres 역할이라면 Cloudtype 쪽이 단순하다. **변수는 무료 1GB 메모리를 앱(JVM 200-300MB)과 DB가 나눠 써야 한다는 점**
- **선행 조건**: 아직 git 저장소가 아니다. Cloudtype은 GitHub에서 코드를 가져가므로 `git init` → GitHub push가 먼저다. Windows에서 push할 때 `git update-index --chmod=+x gradlew`를 잊으면 리눅스 빌드가 `Permission denied`로 실패한다
- **빌드/시작 명령어**: `./gradlew clean bootJar` / `java -jar build/libs/secure-bulletin-board-1.0.0.jar` (`-plain.jar`을 고르면 실행되지 않는다), JDK **17** 선택

**프론트엔드 방향 (질문만 나온 상태)**

정적 HTML/CSS/JS로 다시 만드는 안이 거론됐으나, 그러면 XSS 이스케이프·CSRF 토큰·미인증 401 처리를 전부 직접 해야 해서 **이 프로젝트가 증명하려는 방어를 스스로 약화시킨다.** React를 뺀 것과 같은 이유다. 작은 JS는 `static/js/app.js` 방식(외부 파일 + `data-*` 속성)으로 얼마든지 추가할 수 있다.

**남은 것**
- 포트폴리오 문서 마무리 — [AI_3줄.md](AI_3줄.md), [포트폴리오_추가용_소개글.md](포트폴리오_추가용_소개글.md)는 초안이 작성돼 있으나 **본인 검토가 필요**하다
- 배포 (하지 않기로 했지만, 필요해지면 [docs/05](docs/05_배포.md) 2단계 절차)
- 기능 확장을 한다면 [docs/01 §12](docs/01_기획.md)의 댓글·좋아요·파일첨부. 댓글은 `Post.isAuthoredBy()` 패턴을 그대로 복제하면 된다

### 반드시 지킬 것
- **경로에 `/api`를 붙이지 말 것**: `application.yml`의 `context-path: /api`가 매칭 전에 제거된다. 컨트롤러 매핑과 `SecurityConfig` 매처는 `/posts`, 실제 URL은 `/api/posts`다. (1주차에 이걸 틀려서 인가 규칙이 통째로 빗나갔다)
- **CSRF를 끄지 말 것**: 1주차에 한 번 껐다가 되돌렸고, 그 사실이 [트러블슈팅.md](트러블슈팅.md)에 기록돼 있다
- **인가는 Service 계층에서**: 소유자 비교는 `Post.isAuthoredBy(userId)` 한 곳만 쓴다. 새 기능(댓글 등)을 붙일 때도 같은 패턴을 복제할 것
- **본문 출력은 `th:text`만**: `th:utext`로 바꾸는 순간 XSS 방어가 풀린다. `post/detail.html`에 경고 주석이 달려 있다
- **인라인 `onclick`·`onsubmit`을 쓰지 말 것**: CSP(`script-src 'self'`)가 조용히 막는다. 오류가 화면에 안 보여서 "기능이 원래 없는 것"처럼 지나간다. 동작은 `static/js/app.js`에 두고 HTML에는 `data-*` 속성으로 의도만 표시한다 (실제로 삭제 확인창이 이렇게 죽었던 적이 있다)
- 기능 하나를 끝낼 때마다 [검증안내서.md](검증안내서.md)의 해당 행 "실제 결과" 칸을 채울 것 — 자동 테스트 통과만으로 끝내지 말 것

### 알려진 미해결 사항 (의도된 상태이므로 "버그"로 오해하지 말 것)
- **미인증 요청이 401이 아니라 로그인 페이지로 302된다.** 서버 렌더링 화면에서는 이게 정상 동작이다. 나중에 JS 클라이언트를 붙인다면 `/api` 전용 `AuthenticationEntryPoint`로 401 JSON을 반환하도록 나눠야 한다
- **H2가 인메모리(`create-drop`)라 재시작하면 데이터가 사라진다.** 시연이 필요하면 `jdbc:h2:file:./data/testdb`로 바꾸면 된다
- **ZAP 스캔에 Medium 1건이 남아 있다.** context-path 밖 Tomcat 404라 앱이 헤더를 붙일 수 없어 수용한 것이다. 근거는 [트러블슈팅](트러블슈팅.md)에 있다 — 없애려고 억지로 손대지 말 것
- **Postman 콜렉션이 로컬 파일과 클라우드 두 벌 있다.** 자동 동기화되지 않으므로 고치면 양쪽 다 반영해야 한다

---

## 7. 미해결 질문

- [x] ~~**배포처 확인 필요**~~ **해결 (2026-08-20)**: 로컬 개발 전용으로 전환. 배포는 4주차 이후 선택 사항이며 Vercel(React) + Fly.io(Spring Boot) + Supabase(DB) 조합으로 [docs/05_배포.md](docs/05_배포.md)에 정리됨
- [x] ~~**API 경로의 미인증 응답 형식**~~ **해결 (2026-08-20)**: React를 제거해 JSON 클라이언트가 없어졌으므로 302 리다이렉트가 정상 동작이다. JS 클라이언트를 다시 붙일 때 재검토
- [x] ~~**인증 화면을 React로 옮길지 여부**~~ **해결 (2026-08-20)**: React 자체를 제거하고 Thymeleaf 단일 스택으로 확정
- [ ] 회원가입 시 이메일 인증(메일 발송)을 MVP 범위에 넣을지, 4주 안에는 생략할지
- [ ] 비밀번호 정책(최소 8자, 영문+숫자)을 더 엄격하게(특수문자 포함 등) 할지 여부

---

## 8. 하지 말 것

- 4주 MVP 범위 밖 기능(댓글, 파일 업로드, 관리자 페이지, JWT 전환 등 — [docs/01_기획.md §12](docs/01_기획.md#12-향후-확장-계획) 참고)을 먼저 만들지 않는다.
- "일단 동작하게" CSRF를 끄거나 XSS 필터를 생략하지 않는다 — 이 프로젝트의 존재 이유가 보안이다.
- 인가 검증을 프론트엔드(버튼 숨김 등)로만 처리하지 않는다 — 반드시 서버(Service 계층)에서 재검증한다.
