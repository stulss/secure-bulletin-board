# 트러블슈팅

**관련 문서:** [README](README.md) · [작업내역_체크리스트](작업내역_체크리스트.md) · [docs/01_기획.md](docs/01_기획.md)

> 이 문서는 구현을 진행하며 실제로 마주친 문제를 "문제 / 시도 / 비교 / 배운 점" 형식으로 기록하는 곳입니다.
> 겪지 않은 문제를 미리 지어내지 않습니다 — 실제로 막혔던 순간에만 항목을 추가하세요.

## 1주차에 실제로 겪은 문제

### CSRF를 끈 채로 시작해서, 프로젝트 목적과 정면으로 충돌했다

- **문제**: `SecurityConfig`를 처음 작성할 때 "REST API 테스트 편의"를 이유로 `.csrf(csrf -> csrf.disable())`을 넣었다. 이 프로젝트의 존재 이유가 CSRF·XSS·BCrypt 방어를 증명하는 것인데, 첫 커밋부터 방어 하나를 꺼버린 셈이었다.
- **시도**: CSRF를 다시 켜고, H2 콘솔만 `ignoringRequestMatchers("/h2-console/**")`로 예외 처리했다(H2 콘솔은 자체 폼에 토큰을 넣지 못하고, 개발 전용 경로다). 그다음 "토큰 없이 POST하면 403"을 테스트로 고정해서 다시 꺼지지 않도록 만들었다.
- **비교**: 끄기 전 — 토큰 없는 `POST /auth/signup`이 302로 성공. 켠 후 — 같은 요청이 403, 브라우저 폼(Thymeleaf가 `_csrf` hidden을 자동 삽입)에서는 정상 가입.
- **배운 점**: "일단 되게 하려고" 끈 보안 설정은 대체로 다시 켜지지 않는다. 끄고 싶어질 때가 바로 테스트로 고정할 타이밍이다.

### 한글 폴더명 때문에 빌드 스크립트가 실행되지 않았다

- **문제**: 프로젝트 폴더가 `보안게시판_SecureBulletinBoard`였는데, 개발 서버 실행이 "지정된 경로를 찾을 수 없습니다"로 실패했다. 경로는 분명히 존재했다.
- **시도**: cmd의 코드페이지가 한글 경로를 제대로 넘기지 못하는 것으로 좁혔다. 폴더명을 `secure-bulletin-board`로 바꾸려 했으나 이번엔 "다른 프로세스가 사용 중"이라며 이름 변경이 거부됐다. 하위 폴더는 전부 잠겨 있지 않았고 최상위 폴더만 잠긴 상태였는데, 원인은 **Obsidian이 이 폴더를 볼트로 열어두고 있었기 때문**이었다. Obsidian을 정상 종료한 뒤에도 잠금이 남아, 최종적으로 `robocopy /E /MOVE`로 내용물을 새 폴더로 옮기고 빈 원본을 삭제했다.
- **비교**: 변경 전 — `cmd /c cd "보안게시판_..." && gradlew.bat` 실패. 변경 후 — `./gradlew bootRun`, `npm run dev` 모두 정상 실행.
- **배운 점**: 빌드 도구가 지나가는 경로는 ASCII로 둔다. 그리고 Windows에서 폴더 이름 변경이 막히면, 하위 폴더를 하나씩 이름 변경해봐서 **어느 깊이가 잠겼는지**부터 확인하면 원인을 빠르게 좁힐 수 있다(최상위만 잠겼다면 그 폴더를 열어둔 앱이 범인이다).

### Gradle 9와 Spring Boot 3.3의 플러그인이 맞지 않았다

- **문제**: 로컬에 설치된 Gradle 배포판은 9.5.1인데, `build.gradle`에는 Spring Boot 3.3.2를 적어두어 플러그인이 Gradle 9를 지원하지 않는 조합이 됐다. 시스템에 JDK도 없어서 처음엔 PyCharm 번들 JBR 25로 임시 빌드를 했다.
- **시도**: Temurin JDK 17을 정식 설치하고 `JAVA_HOME`을 사용자 환경변수로 등록했다. Spring Boot를 3.5.6으로 올려 Gradle 9 호환을 맞추고, `sourceCompatibility`는 17로 유지했다.
- **비교**: 임시 조합 — 매번 `JAVA_HOME`을 수동 지정해야 했고 Java 25 런타임이라 검증 가치가 낮았다. 정식 설치 후 — `./gradlew build`, `./gradlew test`가 추가 설정 없이 통과.
- **배운 점**: 빌드가 "일단 돌아가는" 임시 조합(번들 JDK, 버전 불일치)은 검증 결과를 신뢰할 수 없게 만든다. 런타임은 목표 버전으로 맞춰놓고 시작하는 편이 결국 빠르다.

### 미인증 상태의 `/api/auth/me`가 401이 아니라 HTML을 반환했다

- **문제**: React에서 `/api/auth/me`를 `fetch`해 로그인 상태를 표시하려 했는데, 로그아웃 상태에서 JSON 파싱 오류가 났다.
- **시도**: Spring Security의 `formLogin`은 미인증 요청을 **302로 로그인 페이지에 리다이렉트**한다. `fetch`는 리다이렉트를 자동으로 따라가므로 상태 코드는 200, 본문은 로그인 화면 HTML이 된다. 응답의 `content-type`이 `text/html`이면 "로그인 필요"로 처리하도록 고쳤다.
- **비교**: 수정 전 — 로그아웃 상태에서 "백엔드 연결 실패: Unexpected token". 수정 후 — "백엔드 연결됨 · 로그인 필요"로 정상 표시.
- **배운 점**: 서버 렌더링용 인증 설정을 그대로 두고 API를 호출하면, 실패가 4xx가 아니라 200 HTML로 온다. API 경로에는 별도의 `AuthenticationEntryPoint`로 401을 돌려주는 편이 낫다.
- **후속 (2026-08-20)**: React를 제거하고 Thymeleaf 단일 스택으로 확정하면서 이 문제 자체가 사라졌다. 서버 렌더링에서는 302 리다이렉트가 정상 동작이다. 나중에 JS 클라이언트를 다시 붙인다면 이 항목을 먼저 읽고 `/api` 전용 `AuthenticationEntryPoint`부터 넣을 것.

## 기록 형식

새 문제를 기록할 때 아래 형식을 그대로 복사해 채웁니다.

```markdown
### [문제를 한 줄로 요약]

- **문제**: 무엇이 어떻게 안 됐는지, 재현 조건 포함
- **시도**: 원인을 어떻게 좁혀갔는지, 무엇을 바꿔봤는지
- **비교**: 수정 전/후 실제 동작·수치 차이
- **배운 점**: 다음에 비슷한 상황에서 무엇을 먼저 확인할지
```

## 4주차 — OWASP ZAP 스캔에서 겪은 문제

### ZAP이 기동조차 하지 않았다 — 프록시 포트가 백엔드와 같았다

- **문제**: `zap.bat -cmd -quickurl ...` 실행 시 `Failed to start the main proxy: java.net.BindException Address already in use` 로 즉시 종료됐다.
- **시도**: ZAP은 자기 자신이 프록시 서버이고 **기본 포트가 8080**인데, 검사 대상인 Spring Boot도 8080을 쓰고 있었다. `-port 8090`으로 ZAP 프록시만 옮겨서 해결했다. (그 전에 `java`가 PATH에 없어 `zap.bat`이 실패한 문제도 있었는데, JDK의 `java.exe` 절대 경로로 jar를 직접 실행해 우회했다.)
- **비교**: 변경 전 — 즉시 종료. 변경 후 — 스파이더·능동 스캔 정상 완료, HTML 보고서 생성.
- **배운 점**: 검사 도구가 스스로 포트를 점유하는 종류(프록시형)라면, 검사 대상과 포트가 겹치지 않는지부터 본다.

### ZAP이 지적한 3건을 실제로 고쳤다 (Medium 2 / Low 1 → Medium 1)

- **문제**: 1차 스캔에서 세 가지가 나왔다.
  1. `CSP Header Not Set` — 응답에 Content-Security-Policy가 아예 없었다
  2. `Sub Resource Integrity Attribute Missing` — Bootstrap을 CDN에서 `integrity` 없이 불러왔다. CDN이 뚫리거나 중간에서 바뀌어도 브라우저가 알아채지 못한다
  3. `Cookie without SameSite Attribute` — JSESSIONID에 SameSite가 없어, 외부 사이트가 유발한 교차 출처 요청에도 세션 쿠키가 실린다

- **시도**:
  1. **Bootstrap을 CDN에서 `/css/bootstrap.min.css`로 내려받아 로컬화**했다. SRI 해시를 붙이는 방법도 있었지만, 로컬로 두면 SRI 지적이 사라지는 동시에 CSP를 `'self'`로 좁힐 수 있어 둘을 한 번에 해결한다.
  2. `SecurityConfig`에 CSP를 추가했다.
  3. `application.yml`에 `same-site: lax`, `http-only: true`를 넣었다. (`secure: true`는 로컬이 http라 켜면 로그인이 안 되므로 배포 시 활성화하도록 주석으로 남겼다.)

- **비교**: 2차 스캔에서 Low 0, Medium 1로 줄었다. 남은 1건은 내가 CSP에 넣은 `style-src 'unsafe-inline'` 자체를 ZAP이 지적한 것이었다 — 템플릿의 인라인 `style="..."` 속성 때문에 어쩔 수 없이 열어둔 값이었다. 그래서 인라인 스타일을 전부 `/css/app.css`의 클래스로 옮기고 `unsafe-inline`을 제거했다. 브라우저에서 `document.querySelectorAll('[style]').length === 0`, 레이아웃 정상 동작을 확인했다.

- **배운 점**: `'unsafe-inline'`은 "일단 CSP를 넣었다"는 알리바이를 만들어주지만, 정작 CSP가 막아야 할 주입 경로를 그대로 열어둔다. 인라인 스타일을 걷어내는 작업이 선행되어야 CSP가 의미를 갖는다. 스캐너가 이걸 정확히 짚어준 것이 유용했다.

### CSP를 조이자 삭제 확인창이 조용히 사라졌다 (직접 만든 회귀)

- **문제**: `style-src`에서 `'unsafe-inline'`을 걷어내면서 `script-src`도 `'self'`로 두었는데, `post/detail.html`의 삭제 폼이 인라인 이벤트 핸들러를 쓰고 있었다.
  ```html
  <form ... onsubmit="return confirm('이 게시글을 삭제할까요?');">
  ```
  **CSP는 인라인 이벤트 핸들러도 막는다.** 그 결과 확인창이 뜨지 않고 삭제 버튼을 누르는 즉시 글이 지워지는 상태가 됐다. 화면에는 아무 오류도 보이지 않았다.

- **시도**: 브라우저에서 `typeof form.onsubmit`을 확인하니 `"object"`(= null)였다. 핸들러가 함수로 컴파일조차 되지 않은 것이다. 콘솔에 근거가 있었다.
  ```
  Executing inline event handler violates the following Content Security Policy
  directive 'script-src 'self''. ... The action has been blocked.
  ```
  동작을 `static/js/app.js`로 옮기고, HTML에는 `data-confirm` 속성으로 의도만 남겼다. 스크립트는 `'self'`에서 로드되므로 CSP를 통과한다.

- **비교**: 수정 전 — 확인창 없이 즉시 삭제. 수정 후 — 확인창이 뜨고 취소하면 제출이 막힌다(`submissionPrevented: true`로 확인). 서버가 내려주는 HTML을 정규식으로 전수 검사해 `on*=` 형태의 인라인 핸들러가 **0건**임도 확인했다.

- **배운 점**: **CSP 위반은 조용하다.** 콘솔을 열어보지 않으면 "기능이 원래 없는 것"과 구분되지 않는다. `'unsafe-inline'`을 제거할 때는 `style` 속성뿐 아니라 **`onclick`·`onsubmit` 같은 인라인 핸들러도 함께 찾아야** 한다. 그리고 이런 UI 장치는 자동 테스트가 잡아주지 않는다 — 테스트 36건과 API 단언 39건이 전부 통과하는 동안에도 이 회귀는 살아 있었다. 브라우저에서 직접 눌러보는 검증이 여전히 필요한 이유다.

### 끝까지 남은 지적 1건은 고치지 않고 수용했다

- **문제**: 최종 스캔에도 `CSP Header Not Set` 2건이 남았다.
- **시도**: 어느 URL인지 확인해보니 `http://localhost:8080/` 처럼 **context-path(`/api`) 바깥 경로**였다. 이 요청은 Spring Security 필터 체인을 거치지 않고 Tomcat이 직접 404를 돌려주므로, 애플리케이션 코드가 헤더를 붙일 방법이 없다. `/api` 아래 응답(HTML·CSS·404·403 포함)에는 CSP가 모두 붙는 것을 URL별로 직접 확인했다.
- **비교**: `/api/posts`, `/api/auth/login`, `/api/css/app.css`, `/api/posts/999999`(404) — 전부 CSP 있음. `/`, `/api/`(컨텍스트 루트 밖) — 없음.
- **배운 점**: 스캐너 지적을 0으로 만드는 것 자체가 목적이 되면 억지 수정을 하게 된다. 왜 발생하는지 확인하고, 실제 위험과 배포 시 사라질 조건을 근거로 **수용 결정을 기록으로 남기는 편**이 낫다. 배포 시에는 앱이 호스트 루트나 리버스 프록시 뒤에 놓여 이 경로 자체가 없어진다.
- **후속 (2026-08-21)**: 실제로 배포하면서 `context-path: /api` 를 완전히 제거했다(다른 이유 — 루트 접속이 게시판으로 바로 이동하게 하려고).
  그 결과 이 지적이 근거로 삼았던 "context-path 밖 경로"가 아예 사라졌고, 재스캔 결과 **Medium 0건**이 됐다.
  당시 "배포 시 사라질 조건"이라고 적어둔 예상이 그대로 맞아떨어진 사례다 — 수용 결정을 내릴 때 "왜 사라지는지"까지 적어두면,
  나중에 그 조건이 실제로 바뀌었을 때 다시 판단할 필요 없이 바로 확인만 하면 된다.

## 배포 후 — Cloudtype에서 겪은 문제

### 루트(`/`)로 접속하면 404가 떴다 — 원인은 코드가 아니라 `context-path` 였다

- **문제**: Cloudtype에 배포하고 발급된 도메인 루트로 접속하니 404가 떴다. `HomeController`에 `@GetMapping("/")`가 이미 있는데도 그랬다.
- **시도**: 404 화면이 Spring이 만든 에러 페이지가 아니라 **톰캣 기본 404 페이지**라는 게 단서였다. `application.yml`에 `server.servlet.context-path: /api`가 설정돼 있어, 애플리케이션이 `/api`로 시작하는 요청만 처리했다. `/`로 들어온 요청은 스프링 디스패처까지 도달하지 못하고 톰캣이 먼저 응답을 끝내버려, `HomeController`의 `/` 매핑 자체가 무력화된 상태였다. `/api/posts`로 접속하면 정상적으로 게시판이 떴다는 게 증거였다.
- **비교**: `context-path: /api`를 제거하니 루트(`/`) 접속이 `HomeController` → `redirect:/posts`로 정상 흐른다. 모든 링크가 Thymeleaf `@{/posts}` 문법으로 작성돼 있어 context-path 유무와 무관하게 자동으로 올바른 경로를 렌더링했으므로, 하드코딩된 `href`를 고칠 필요가 없었다. `SecurityConfig`의 URL 매처도 원래 context-path를 제외한 상대 경로(`/posts`, `/auth/login` 등)로 적혀 있어 수정이 필요 없었다.
- **부수 효과**: ZAP이 "context-path 밖 경로라 CSP를 못 붙인다"는 이유로 수용해뒀던 Medium 지적이, context-path 자체가 사라지며 함께 해결됐다(재스캔 결과 Medium 0건). 자세한 내용은 위 "끝까지 남은 지적 1건" 항목의 후속 기록 참고.
- **배운 점**: 404가 스프링이 아니라 서버(WAS)가 낸 것인지부터 구분하면 원인 후보가 절반으로 줄어든다. 톰캣 기본 404 페이지와 스프링의 `error/error.html`은 디자인이 다르므로 눈으로 구분된다. 그리고 라우팅에 `@{...}` 같은 context-path-relative 문법을 처음부터 일관되게 쓰면, 나중에 context-path를 통째로 넣거나 뺄 때 설정 한 줄로 끝난다 — 하드코딩된 절대 경로였다면 파일마다 고쳐야 했을 것이다.

## Spring Security 관련 흔한 함정 (미리 알아두면 좋은 것 — 실제 발생 시 위 형식으로 재기록)

아래는 이 프로젝트 유형에서 흔히 보고되는 함정 목록입니다. 실제로 겪게 되면 위 기록 형식으로 옮겨 정식 항목으로 만드세요.

- CSRF를 "일단 되게 하려고" `.csrf(csrf -> csrf.disable())`로 꺼버리고 그대로 방치하는 경우
- `@PreAuthorize`만 걸고 Service 계층 소유자 검증을 빠뜨려, 다른 진입 경로에서 인가가 우회되는 경우
- 로그아웃 시 `invalidateHttpSession(true)`를 빠뜨려 세션이 계속 살아있는 경우
- Thymeleaf에서 `th:text` 대신 `th:utext`를 실수로 써서 이스케이프가 풀리는 경우
- JPA `@Query`에 `nativeQuery = true`와 문자열 결합을 같이 써서 파라미터 바인딩 이점이 사라지는 경우
