# 검증안내서

**관련 문서:** [README](README.md) · [docs/01_기획.md](docs/01_기획.md) · [작업내역_체크리스트](작업내역_체크리스트.md)

> **현재 상태: 4주차 범위까지 검증 완료** — 인증(BCrypt·CSRF·세션), 게시글 CRUD, 작성자 본인 확인(403),
> XSS 이스케이프, SQL Injection, OWASP ZAP. 실제 Cloudtype 배포까지 완료됨.
>
> ⚠️ **2026-08-21 부터 `context-path: /api` 를 제거했다.** 아래 표의 "실제 결과" 칸에 남은 `/api/auth/...`,
> `/api/posts` 같은 경로는 **당시 실행 시점 기준 기록**이다. 지금은 `/api` 접두사 없이 동일하게 동작한다
> (예: `/api/auth/login` → `/auth/login`). 새로 검증할 때는 접두사 없는 경로를 쓸 것.

## 실행 방법

```bash
./gradlew bootRun
```

http://localhost:8080 에서 뜬다. 화면까지 Thymeleaf가 서버에서 렌더링하므로 별도 프론트엔드 서버는 없다.

## 검증 시나리오

### 1. 회원가입 / 로그인 / 로그아웃
| 절차 | 기대 결과 | 실제 결과 (2026-08-20) |
|---|---|---|
| `/api/auth/signup`에서 신규 이메일로 가입 | 가입 성공, 로그인 화면으로 이동 | ✅ 302 → `/api/auth/login?signup=true`, "회원가입이 완료되었습니다" 표시 |
| 동일 이메일로 재가입 시도 | 중복 오류 메시지, 가입 거부 | ✅ `IllegalArgumentException("이미 사용 중인 이메일입니다")` — `UserServiceTest` 통과 |
| 동일 사용자명으로 재가입 시도 | 중복 오류 메시지, 가입 거부 | ✅ `UserServiceTest` 통과 |
| 정상 아이디/비밀번호로 로그인 | 세션 발급, 인증 상태가 됨 | ✅ 브라우저에서 로그인 후 `/api/auth/me`가 `{"username":"hongildong"}` 반환 |
| 틀린 비밀번호로 로그인 | 인증 실패, 오류 표시 | ✅ 302 → `/api/auth/login?error=true`, 미인증 상태 유지 |
| 로그아웃 클릭 | 세션 무효화, 이후 인증 필요 페이지 접근 시 로그인 화면으로 리다이렉트 | ✅ 302 → `/api/auth/login?logout=true`, 이후 `/posts/new` 접근이 로그인 화면으로 리다이렉트 |
| 로그인 전후의 JSESSIONID 비교 | 로그인 시 세션 ID가 새로 발급됨 (세션 고정 공격 방지) | ✅ `872E05AD…` → `2DAD4F92…` 로 재발급 확인 |

### 2. 게시글 CRUD 및 권한 제어
| 절차 | 기대 결과 | 실제 결과 (2026-08-20) |
|---|---|---|
| 비로그인 상태로 글 목록/상세 열람 | 정상 열람 가능 | ✅ 목록·상세 모두 200 |
| 비로그인 상태로 글쓰기 페이지 접근 | 로그인 화면으로 리다이렉트 또는 401/403 | ✅ 302 → `/api/auth/login` |
| 로그인 후 글 작성 | 목록에 즉시 반영 | ✅ 302 → `/api/posts/1`, 목록에 노출 확인 |
| 제목을 비우고 작성 | 저장되지 않고 폼으로 돌아오며 오류 표시 | ✅ `post/form` 재표시 + 필드 오류 (`PostControllerTest`) |
| 작성자 본인이 자기 글 수정/삭제 | 정상 처리 | ✅ 수정 후 내용 반영, 삭제 후 목록으로 이동 |
| A 계정으로 로그인한 상태에서 B의 글 ID를 URL에 직접 넣어 **수정** 시도 | **403 Forbidden**, 실제로 변경되지 않음 | ✅ 403, 이후 조회 시 원래 제목 유지("탈취" 문자열 없음) |
| A 계정으로 로그인한 상태에서 B의 글 ID를 URL에 직접 넣어 **삭제** 시도 | **403 Forbidden**, 글이 남아있음 | ✅ 403, 글 그대로 존재 |
| 존재하지 않는 글 ID 조회 | 404 | ✅ 404 |

### 3. 보안 방어 확인
| 절차 | 기대 결과 | 실제 결과 (2026-08-20) |
|---|---|---|
| 게시글 본문에 `<script>alert(1)</script>` 입력 후 등록 | 상세 페이지에서 alert가 뜨지 않고, 텍스트 그대로(이스케이프되어) 표시됨 | ✅ 응답 HTML에 원본 태그 없음 / `&lt;script&gt;`로 이스케이프. 브라우저 DOM 확인 결과 `article script` 요소 **0개**, 본문은 자식 요소 0개인 순수 텍스트 |
| CSRF 토큰 없이 `POST /api/posts` 직접 호출 | 403 Forbidden | ✅ 403 (실제 서버 + `PostControllerTest` 양쪽 확인) |
| 로그인 아이디 필드에 `' OR '1'='1` 입력 | 로그인 실패, 서버 오류 없이 정상적인 실패 응답 | ✅ 미인증 상태 유지 (`SecurityDefenseTest`) |
| 조회 조건에 `victim'; DROP TABLE users; --` 입력 | 문자열 리터럴로 취급되어 테이블·데이터가 그대로 보존됨 | ✅ 조회 결과 없음, `users` 테이블과 기존 행 보존 확인 |
| Controller를 거치지 않고 Service를 직접 호출 | `@PreAuthorize`가 인증 없는 호출을 거부 | ✅ `AccessDeniedException` 발생 (`SecurityDefenseTest`) |
| 회원가입 후 `users.password` 컬럼을 직접 조회 | `$2a$`로 시작하는 BCrypt 해시 값이며 평문이 아님 | ✅ `UserServiceTest`가 저장된 값이 평문과 다르고 `^\$2[aby]\$\d{2}\$.{53}$` 형식임을 단언 — 통과 |
| 같은 비밀번호로 두 사용자 가입 후 해시 비교 | 서로 다른 해시 (salt 적용) | ✅ `UserServiceTest` 통과 |
| CSRF 토큰 없이 `POST /auth/signup` 직접 호출 | 403 Forbidden | ✅ `AuthControllerTest` 통과 (토큰이 있으면 302 성공) |
| 미인증 상태로 보호된 경로 접근 | 로그인 화면으로 리다이렉트 | ✅ `AuthControllerTest` 통과 |

### 4. 자동 테스트
| 절차 | 기대 결과 | 실제 결과 (2026-08-20) |
|---|---|---|
| `./gradlew test` 실행 | JUnit 5 단위 테스트 + `@SpringBootTest` 통합 테스트 전부 통과 | ✅ **36건 전부 통과** — `UserServiceTest` 4 · `AuthControllerTest` 6 · `PostServiceTest` 8 · `PostControllerTest` 13 · `SecurityDefenseTest` 5 |
| Postman 콜렉션 실행 (newman) | 인증·CRUD·보안 시나리오 단언 전부 통과 | ✅ **단언 39건 전부 통과** (requests 40, failed 0) — [docs/04](docs/04_API테스트_Postman.md) |
| OWASP ZAP으로 전체 스캔 | High 등급 취약점 0건 (또는 발견 시 [트러블슈팅.md](트러블슈팅.md)에 대응 기록) | ✅ **High 0 · Medium 0 · Low 0** (2026-08-21, context-path 제거 후 재스캔) — 보고서: `security/zap-report.html` |

### ZAP 스캔 결과 추이 (OWASP ZAP 2.17.0, 스파이더 + 능동 스캔)

| 회차 | High | Medium | Low | 조치 |
|---|---|---|---|---|
| 1차 | 0 | 2 | 1 | CSP 헤더 없음 / SRI 속성 누락 / 쿠키 SameSite 없음 |
| 2차 | 0 | 1 | 0 | Bootstrap 로컬화(SRI 해소), CSP 추가, SameSite=Lax 적용. 남은 1건은 `style-src 'unsafe-inline'` |
| 3차 | 0 | 1 | 0 | 인라인 `style` 속성을 `/css/app.css`로 이관 후 `unsafe-inline` 제거. 남은 1건은 context-path 밖 경로 문제(아래 "지난 기록") |
| **4차 (2026-08-21, 최종)** | **0** | **0** | **0** | `context-path: /api` 를 제거하며 **부수적으로 해결됨** — Informational 2건만 남음 |

**지난 기록 — 한때 "수용한 지적"이었던 것 (지금은 해결됨)**

3차까지는 `Content Security Policy (CSP) Header Not Set` 이 남아 있었다. 해당 URL이 **context-path(`/api`) 바깥 경로**(`http://localhost:8080/` 등)였기 때문에,
Spring Security 필터 체인을 거치지 않고 Tomcat이 직접 404를 반환해 애플리케이션이 헤더를 붙일 수 없었다.
당시에는 "배포 시 앱이 호스트 루트에 놓이면 이 경로 자체가 사라진다"는 근거로 수용 결정을 내렸는데,
**2026-08-21에 context-path를 아예 제거**하면서 그 경로 자체가 없어져 지적도 함께 사라졌다. 예상이 실제로 맞아떨어진 사례다.

**적용된 보안 헤더**

```
Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self';
                         img-src 'self' data:; form-action 'self';
                         frame-ancestors 'self'; base-uri 'self'; object-src 'none'
Set-Cookie: JSESSIONID=...; Path=/; HttpOnly; SameSite=Lax
```

## 통과 기준 요약

- 위 표의 "기대 결과"가 실제 화면/응답/DB 값으로 **직접 확인**될 것 (자동 테스트 통과만으로 끝내지 않는다 — [odd-one-out 프로젝트](../odd-one-out/README.md)에서 "통과하는 검사가 사실 아무것도 검사하지 않던" 사례가 있었으므로, 이 프로젝트에서도 실패 케이스를 실제로 재현해 확인한다)
- 실행 결과는 이 문서에 표/스크린샷 경로로 갱신한다.
