# 포트폴리오 추가용 소개글

**관련 문서:** [README](README.md) · [docs/01_기획.md](docs/01_기획.md) · [트러블슈팅](트러블슈팅.md) · [검증안내서](검증안내서.md)

> **이 파일의 용도**: 개인 포트폴리오를 작업하는 다른 대화창에 이 파일 내용을 그대로 붙여넣으면,
> 이 프로젝트를 포트폴리오에 한 항목으로 추가할 수 있습니다.
>
> **현재 상태 (2026-08-20): 개발 완료, 로컬 실행.** 아래 수치는 전부 실제 실행 결과다.
> 배포는 하지 않기로 결정했으므로 배포 주소 자리는 비워두었다 — 없는 주소를 지어내지 말 것.

---

## 1. 프로젝트 요약 (사람이 읽는 버전)

**Spring Security 기반 보안 게시판** — 인증/인가와 웹 취약점 방어를 직접 구현하고, 방어가 실제로 동작함을 테스트로 증명한 게시판

### 한 줄 소개

회원가입·로그인·게시글 CRUD를 갖춘 게시판에, **"본인 글만 수정·삭제된다"를 서버에서 실제로 증명하는 것**을 목표로 만들었습니다.
버튼을 숨기는 대신, 다른 사용자의 글 ID를 URL에 직접 넣어 요청해도 403이 반환되고 **데이터가 실제로 바뀌지 않는지까지** 테스트로 고정했습니다.

### 사용 기술

Java 17 · Spring Boot 3.5.6 · Spring Security · Spring Data JPA · Thymeleaf · Bootstrap 5 · H2 · Gradle 9.5
테스트·검증: JUnit 5, Spring Security Test, MockMvc, Postman/newman, OWASP ZAP 2.17

### 핵심 3가지

**1. 인가를 세 겹으로 나누고, 각 겹이 답하는 질문을 분리했습니다**

| 계층 | 답하는 질문 | 막는 것 |
|---|---|---|
| `SecurityConfig` URL 매처 | 로그인했는가 | 비로그인 사용자의 작성·수정·삭제 |
| `@PreAuthorize` 메서드 보안 | 로그인했는가 | Controller를 우회한 Service 직접 호출 |
| `PostService`의 소유자 검증 | **본인 글인가** | 로그인은 했지만 남의 글인 경우 |

앞의 두 겹은 "로그인 여부"만 답합니다. **"본인 글인가"는 세 번째 겹만 답할 수 있고**, 이 프로젝트가 증명하려는 것이 정확히 그 지점입니다.
소유자 비교는 `Post.isAuthoredBy(userId)` 한 곳에만 두어, 수정·삭제·폼 접근·버튼 노출이 각자 다른 방식으로 판단하다 한 군데만 틀리는 상황을 막았습니다.

**2. 평문 비밀번호 0개 — 저장 형태를 테스트로 고정했습니다**

`BCryptPasswordEncoder`로 해싱해 저장하고, 저장된 값이 평문과 다르고 `$2[aby]$` 형식의 60자인지, **같은 비밀번호라도 사용자마다 해시가 다른지(salt)** 를 단위 테스트로 검증합니다.
"암호화했다"는 주장 대신, 깨지면 빌드가 실패하는 형태로 남겼습니다.

**3. 방어를 껐다 켜지 않았습니다**

작업 중 CSRF를 "테스트 편의"로 한 번 껐다가, 이 프로젝트의 존재 이유와 충돌한다고 판단해 되돌렸습니다.
되돌리면서 **"토큰 없는 POST는 403"을 테스트로 고정**해, 다시 꺼지지 않도록 만들었습니다. 이 판단 과정은 트러블슈팅 문서에 그대로 남겼습니다.

### 검증 결과 (전부 실제 실행)

| 항목 | 결과 |
|---|---|
| JUnit 5 + MockMvc 통합 테스트 | **36건 전부 통과** |
| Postman 콜렉션 (newman) | **39개 단언 전부 통과** (요청 25개) |
| OWASP ZAP 스파이더 + 능동 스캔 | **High 0건 · Low 0건** |

실행 중인 서버에 직접 확인한 것:

- 다른 사용자의 글을 URL로 직접 수정·삭제 시도 → **403**, 이후 조회 시 **원래 내용 그대로**
- 본문에 `<script>alert(1)</script>` 저장 → 브라우저 DOM에 `script` 요소 **0개**, 텍스트로만 렌더링
- CSRF 토큰 없는 POST → **403**
- 로그인 아이디에 `' OR '1'='1` → 인증 실패, `'; DROP TABLE users; --` → 리터럴로 취급되어 테이블 보존
- 로그인 시 JSESSIONID **재발급**(세션 고정 방지), 로그아웃 후 보호 페이지 접근 차단

**적용한 보안 헤더**

```
Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self';
                         img-src 'self' data:; form-action 'self';
                         frame-ancestors 'self'; base-uri 'self'; object-src 'none'
Set-Cookie: JSESSIONID=...; HttpOnly; SameSite=Lax
```

CSP에 `'unsafe-inline'`이 없습니다. 그래서 Bootstrap을 CDN이 아니라 로컬에서 서빙하고, 템플릿에 인라인 `style` 속성을 쓰지 않습니다.

### 배포 주소 / 소스 코드

- **배포 주소**: _배포하지 않음._ 로컬 실행 기준으로 완성했고, 배포 절차는 [docs/05_배포.md](docs/05_배포.md)에 정리되어 있습니다 (코드 변경 없이 환경변수·DB 연결만 교체).
- **소스 코드**: _(저장소를 만들면 여기에 주소를 넣을 것)_

---

## 2. 이 프로젝트에서 얻은 것

실제로 막혔던 지점만 적습니다. 전부 [트러블슈팅.md](트러블슈팅.md)에 기록이 남아 있습니다.

**"일단 되게 하려고" 끈 보안 설정은 다시 켜지지 않는다**
CSRF를 비활성화한 채로 시작했다가 되돌린 경험에서, **끄고 싶어지는 순간이 테스트로 고정할 타이밍**이라는 걸 배웠습니다. 지금은 토큰 없는 요청이 403인지 검사하는 테스트가 있어서, 누가 다시 끄면 빌드가 깨집니다.

**`'unsafe-inline'`은 CSP를 넣었다는 알리바이만 만든다**
ZAP이 1차 스캔에서 CSP 부재를 지적해 헤더를 추가했더니, 2차 스캔에서 이번엔 제가 넣은 `style-src 'unsafe-inline'` 자체를 지적했습니다. 인라인 스타일을 허용하면 주입된 스타일까지 함께 허용되어, CSP가 막아야 할 경로가 그대로 열립니다. 템플릿의 인라인 `style` 속성을 전부 CSS 파일로 옮기고 나서야 `style-src 'self'`로 조일 수 있었습니다. **선행 정리 없이 헤더만 붙이는 건 의미가 없다**는 걸 스캐너가 정확히 짚어줬습니다.

**스캐너 지적을 0으로 만드는 것 자체가 목적이 되면 안 된다**
최종 스캔에 Medium 1건이 남았는데, 확인해보니 context-path 밖 경로에 대한 Tomcat 기본 404라 애플리케이션이 헤더를 붙일 수 없는 구간이었습니다. 억지로 없애는 대신 **왜 발생하는지와 배포 시 사라지는 조건을 근거로 수용 결정을 기록**했습니다. 어떤 지적은 고치는 것보다 판단 근거를 남기는 게 맞다고 봅니다.

**403을 받았다는 것만으로는 "막혔다"고 할 수 없다**
서버가 403을 돌려주면서 데이터는 이미 바꿔놨을 수도 있습니다. 그래서 인가 테스트를 **거부 응답 확인 + 데이터 불변 확인** 짝으로 만들었습니다. 단위 테스트와 Postman 콜렉션 양쪽에 같은 원칙을 적용했습니다.

**메서드 보안을 넣자 기존 테스트 9건이 깨졌는데, 그게 정답이었다**
`@PreAuthorize`를 추가하니 인증 컨텍스트 없이 Service를 호출하던 테스트들이 실패했습니다. 어노테이션을 빼서 통과시키는 대신, "인증 없이 Service를 부르면 안 된다"는 새 규칙이 정상 작동한 결과로 보고 **테스트 쪽에 인증 컨텍스트를 부여**했습니다.

---

## 3. 포트폴리오 데이터 형식 (붙여넣기용)

### 3-1. 강점 카드 형식 (상황 / 행동 / 결과)

```javascript
{
  id: 'secure-bulletin-board',
  title: 'Spring Security 기반 보안 게시판',
  summary:
    '인증·인가와 웹 취약점 방어를 직접 구현하고, 방어가 실제로 동작함을 테스트 36건과 ' +
    'API 단언 39건으로 고정했습니다. OWASP ZAP 스캔 High 0건.',
  situation:
    '게시판에서 "본인 글만 수정·삭제 가능"은 프론트엔드에서 버튼을 숨기는 것으로 ' +
    '끝내기 쉽지만, URL을 직접 호출하면 그대로 뚫립니다. 인가가 서버에서 실제로 ' +
    '동작하는지 증명할 수 있는 구조가 필요했습니다.',
  action:
    '인가를 세 겹(URL 매처 / @PreAuthorize / Service 소유자 검증)으로 나누고, ' +
    '각 겹이 답하는 질문을 분리했습니다. 소유자 비교는 엔티티의 판단 메서드 한 곳으로 ' +
    '모아 검증 경로가 흩어지지 않게 했습니다. 인가 테스트는 403 확인에 그치지 않고 ' +
    '데이터가 실제로 바뀌지 않았는지까지 함께 단언하도록 짝으로 구성했습니다.',
  result:
    '타인 글 수정·삭제 시도가 403으로 차단되고 내용이 보존됨을 실행 중인 서버와 ' +
    '테스트 양쪽에서 확인했습니다. JUnit 36건·Postman 39개 단언 전부 통과, ' +
    'OWASP ZAP 스캔 결과 High·Low 0건. 스캔에서 지적된 CSP·SRI·SameSite 3건은 ' +
    '수정했고, 남은 1건은 애플리케이션이 관여할 수 없는 구간이라 근거를 남기고 수용했습니다.',
  evidence: {
    label: '검증 시나리오와 실행 결과',
    url: '(저장소 주소)/blob/main/검증안내서.md',
  },
}
```

### 3-2. 트러블슈팅 형식 (문제 / 시도 / 비교 / 배운 점)

대표 사례 하나만 옮깁니다. 나머지는 [트러블슈팅.md](트러블슈팅.md)에 있습니다.

```javascript
{
  id: 'csp-unsafe-inline',
  title: 'CSP를 넣었는데 스캐너가 다시 지적한 이유',
  problem:
    'OWASP ZAP 1차 스캔에서 CSP 헤더 부재를 지적받아 헤더를 추가했더니, ' +
    '2차 스캔에서 이번엔 그 헤더에 포함된 style-src \'unsafe-inline\' 자체를 지적했습니다.',
  attempt:
    '\'unsafe-inline\'은 템플릿의 인라인 style="..." 속성 때문에 열어둔 값이었습니다. ' +
    '이걸 허용하면 주입된 인라인 스타일까지 함께 허용되어, 정작 CSP가 막아야 할 ' +
    '경로가 그대로 열린다는 걸 확인했습니다. 템플릿 6개의 인라인 스타일을 ' +
    '별도 CSS 파일의 클래스로 옮기고 지시어를 style-src \'self\'로 조였습니다. ' +
    '같은 맥락에서 Bootstrap도 CDN에서 로컬 서빙으로 바꿔 SRI 지적을 함께 해소했습니다.',
  comparison:
    '조치 전 Medium 2건·Low 1건 → 조치 후 Medium 1건·Low 0건. ' +
    '브라우저에서 인라인 style 속성 0개와 레이아웃 정상 동작을 확인했고, ' +
    '기존 테스트 36건과 API 단언 39건에 회귀가 없음을 확인했습니다.',
  lesson:
    '\'unsafe-inline\'이 들어간 CSP는 "헤더를 넣었다"는 형태만 갖출 뿐입니다. ' +
    '인라인 스타일·스크립트를 걷어내는 선행 작업이 있어야 CSP가 실제 방어가 됩니다.',
}
```

---

## 4. 붙여넣을 때 확인할 것

- [ ] `(저장소 주소)`를 실제 주소로 바꿨는가 (아직 저장소가 없다면 그 항목을 통째로 빼는 편이 낫다)
- [ ] **배포 주소를 지어내지 않았는가** — 이 프로젝트는 배포하지 않았다. "로컬 실행 기준 완성"이라고 쓰는 편이 정확하다
- [ ] 수치를 최신 값으로 유지했는가 (테스트 36건 / 단언 39건 / ZAP High 0건 — 코드가 바뀌면 이 숫자도 바뀐다)
- [ ] 겪지 않은 트러블슈팅이 섞여있지 않은가 — [트러블슈팅.md](트러블슈팅.md)에 기록이 있는 것만 쓴다
- [ ] 게시판 화면 스크린샷을 함께 넣을지 정했는가 (목록 화면에 XSS 페이로드가 텍스트로 표시되는 장면이 이 프로젝트의 성격을 한눈에 보여준다)
