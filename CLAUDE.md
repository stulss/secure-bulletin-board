# 보안게시판_SecureBulletinBoard 작업 규칙 (Claude Code 세션 시작 시 자동 로드)

> 이 폴더에서 작업할 때 **폴더 전체를 read하지 말 것.** 아래 규칙을 따를 것.

## 1. 세션 시작 시 가장 먼저 할 일

폴더 목록을 훑어보는 대신, **`작업내역_체크리스트.md` 한 파일만** 먼저 읽는다.
그 파일 안의 `0. AI 필독 — 작업 프로토콜` 섹션이 이후 작업 순서(시작 전/중/종료 시)를 전부 정의한다 — 여기서 중복 설명하지 않는다.

## 2. 절대 통째로 읽지 말 것 (토큰 낭비)

| 경로 | 이유 |
|---|---|
| `build/`, `.gradle/`, `out/` | Gradle 빌드 산출물. 원칙적으로 읽지 않음 (코드 작성 이후 생성 예정) |
| `src/main/resources/static/**`, `src/main/resources/templates/**` 이미지·대량 자산 | 필요해도 개별 파일 1~2개만 |
| `*.csv`, `*.log` | 필요하면 앞부분(head)만 |
| `src/` 전체 | 작업과 관련된 파일 1~2개만 골라 읽는다. 디렉토리 전체를 한 번에 읽지 않는다 |
| `docs/*.pdf`, `*.pptx` 등 바이너리 산출물 | 내용이 궁금하면 대응하는 `.md` 원본이 있는지 먼저 확인 |
| `gradle/wrapper/` | Gradle Wrapper 바이너리·설정. 버전 확인이 필요할 때 `gradle-wrapper.properties` 한 줄만 |
| `build/test-results/`, `build/reports/` | 테스트 결과 XML/HTML. 실패 원인을 볼 때만 해당 파일 1개 |
| `src/main/resources/static/css/bootstrap.min.css` | 서드파티 CSS 23만 자. 절대 읽지 않음 |
| `security/zap-report.html` | ZAP 스캔 보고서. 요약은 [검증안내서](검증안내서.md)에 정리돼 있으니 그쪽을 먼저 볼 것 |

## 3. 작업 유형별로 문서 하나만 읽기

폴더 전체를 뒤지지 말고, 작업 성격에 맞는 문서 하나만 찾아 읽는다.

| 하려는 작업 | 읽을 문서 |
|---|---|
| 기획·기능명세·ERD·API·보안요구사항 확인 | `docs/01_기획.md` |
| 화면·UI/UX·와이어프레임 확인 | `docs/02_UI_UX설계.md` |
| 패키지 구조·계층 규칙·인증/인가 흐름 확인 | `docs/03_백엔드구조.md` |
| API 테스트·Postman 콜렉션·테스트 자동화 확인 | `docs/04_API테스트_Postman.md` |
| 배포 방법·절차 | `docs/05_배포.md` |
| 검증 절차·통과 기준 | `검증안내서.md` |
| 과거 문제 해결 사례 | `트러블슈팅.md` |
| 진행 상황·결정 기록·다음 할 일 | `작업내역_체크리스트.md` |

## 4. 코드 수정 시

- 어떤 파일을 고쳐야 하는지 알고 있다면 그 파일만 연다 (이름을 지정해서).
- 모를 때만 디렉토리 목록을 보되, 목록만 보고 내용을 전부 읽지는 않는다.
- 기존 파일을 통째로 다시 쓰지 않는다 — 필요한 부분만 수정한다.
- **보안 기능(BCrypt, CSRF, XSS 필터, JPA 파라미터 바인딩, 리소스 소유자 검증)을 "일단 되게 하려고" 끄거나 우회하지 않는다.** 이 프로젝트의 핵심 목적이 보안이다.

- **코드를 고쳤으면 그 작업은 아직 끝난 게 아니다.** 관련 문서를 갱신해야 작업이 종료된 것으로 친다.
  - `작업내역_체크리스트.md`의 작업 로그·진행사항 체크리스트에 반영
  - 동작이나 구조가 바뀌었으면 `README.md`, `docs/01_기획.md`, `docs/05_배포.md` 등 해당하는 곳도 같이 수정
  - "나중에 한꺼번에 정리" 하지 않는다 — 코드 변경과 문서 갱신을 같은 작업 단위로 묶는다

#### 4-1. 커밋 전 문서 갱신 체크리스트

코드를 커밋하기 직전, 아래 중 해당하는 항목이 있는데 갱신을 안 했다면 **커밋하지 않는다.**

- [ ] 새 기능/동작을 추가했다 → `README.md` 또는 `docs/01_기획.md`의 해당 설명을 갱신했는가
- [ ] 구조·파일 위치가 바뀌었다 → `README.md`의 폴더 구조·문서 색인을 갱신했는가
- [ ] 배포 방식·설정이 바뀌었다 → `docs/05_배포.md`를 갱신했는가
- [ ] 문제를 발견하고 해결했다 → `트러블슈팅.md`에 문제/시도/결과를 기록했는가
- [ ] 새로운 결정을 내렸다 → `작업내역_체크리스트.md`의 `## 5. 결정 기록`에 추가했는가
- [ ] 어쨌든 뭔가 했다 → `작업내역_체크리스트.md`의 `## 4. 작업 로그`에 방금 한 일을 남겼는가

체크리스트에 걸리는 게 없으면 (순수 리팩터링, 문서와 무관한 내부 정리 등) 그대로 커밋해도 된다 — 이건 문서 갱신을 강제하는 게 아니라, 빠뜨리기 쉬운 순간을 짚어주는 용도다.

## 5. 이 파일 자체 수정 규칙

새로 생긴 대형 폴더/파일(예: `src/` 하위 대량 코드, 빌드 산출물)이 있으면 2번 표에 한 줄 추가한다. 규칙이 과하다 싶으면 사용자와 상의 후 줄인다.
