# AI 3줄

**관련 문서:** [README](README.md) · [검증안내서](검증안내서.md) · [트러블슈팅](트러블슈팅.md) · [작업내역_체크리스트](작업내역_체크리스트.md)

> ⚠️ **제출 전에 본인이 반드시 검토·수정하세요.**
> 아래는 AI(Claude Code)가 **작업 로그와 결정 기록에 남은 사실만으로** 초안을 작성한 것입니다.
> 특히 두 번째·세 번째 항목은 본인의 판단을 서술하는 자리이므로, 실제 의도와 다르게 적힌 부분이 있으면
> 그대로 두지 말고 본인 표현으로 고치세요. 하지 않은 일을 했다고 적으면 문서 전체의 신뢰가 무너집니다.

---

- **AI에게 맡긴 일**: 기획 문서 세트(개발기획서 12섹션, UI/UX 설계, 백엔드 구조, API 테스트 설계, 배포 문서)와 Spring Boot 코드 전체 구현을 맡겼다. 구체적으로는 `User`·`Post` 엔티티와 JPA 매핑, `SecurityConfig`(BCrypt·CSRF·세션 고정 방지·CSP), 인가 3중 방어(URL 매처 / `@PreAuthorize` / Service 소유자 검증), Thymeleaf 화면 6개, 전역 예외 처리, JUnit·MockMvc 테스트 36건, Postman 콜렉션(요청 25개·단언 39개), OWASP ZAP 스캔 실행과 지적사항 수정까지다. 환경 구축(JDK 17·Gradle Wrapper·ZAP 설치)과 문서 갱신도 함께 맡겼다.

- **내가 판단한 일**: **비용 구조와 스택 범위**를 직접 정했다. 처음에 배포처로 Vercel과 Supabase를 제안했다가, AI가 "Vercel은 Java 장기 실행 서버를 올릴 수 없다"고 알려와 재검토했다. 이때 무료 티어의 실제 제약(월 크레딧·슬립 모드)을 되물어 확인한 뒤, **4주 동안은 로컬에서만 개발하고 배포는 나중에 필요할 때 하기로** 결정했다. 이 결정을 내리기 전에 "로컬로만 개발하다가 나중에 배포로 전환 가능한가"를 먼저 확인해, 되돌릴 수 없는 선택이 아님을 검증했다. 개발이 끝난 뒤에는 **React를 제거하고 Thymeleaf 단일 스택으로 확정**했다 — React가 로그인 상태만 표시하는 껍데기로 남아 있어, 포트폴리오에서 "왜 있는지" 설명할 수 없는 코드는 두지 않는 편이 낫다고 봤다. 배포도 하지 않기로 최종 결정했다.

- **AI 말을 안 들은 일**: AI가 **JDK가 설치돼 있지 않자 PyCharm에 번들된 JBR 25로 우회해서 빌드를 통과**시키고 있었다. 당장은 돌아갔지만 목표 버전(Java 17)이 아닌 런타임이라 검증 결과를 신뢰할 수 없는 상태였다. 이걸 중단시키고 **"우회하지 말고 제대로 설치하라"고 지시**했고, 그 결과 Temurin JDK 17을 정식 설치하고 `JAVA_HOME`을 등록해 이후 빌드·테스트가 추가 설정 없이 돌아가게 됐다. 같은 맥락에서 **한글 폴더명이 빌드 스크립트 실패의 원인**임을 지적하고 ASCII로 바꾸게 했다 — AI는 그 전까지 실행 방식을 바꿔가며 우회를 시도하고 있었다. 또 AI가 Docker를 쓸 수 없다고 판단했을 때 "도커도 있다"고 지적해 다시 확인하게 했고, 확인 결과 설정 폴더만 남고 실행 파일·WSL 배포판이 없어 실제로는 쓸 수 없는 상태임이 드러나 네이티브 ZAP으로 진행했다.

---

## 참고: 위 내용의 근거 위치

| 항목 | 근거 |
|---|---|
| 인가 3중 방어·테스트 36건·단언 39건·ZAP 결과 | [검증안내서.md](검증안내서.md) |
| JDK 우회 → 정식 설치, 한글 폴더명 문제 | [트러블슈팅.md](트러블슈팅.md) "한글 폴더명", "Gradle 9와 Spring Boot 3.3" |
| 배포 전략 전환, React 제거, ZAP 지적 수용 | [작업내역_체크리스트.md](작업내역_체크리스트.md) `## 5. 결정 기록` |

> **덧붙일 만한 것**: AI가 스스로 되돌린 판단도 있었다. `SecurityConfig`를 처음 작성할 때 "테스트 편의"로 CSRF를
> 비활성화했다가, 이 프로젝트의 목적과 충돌한다며 다시 켜고 "토큰 없는 POST는 403"을 테스트로 고정했다.
> 이건 본인이 지시한 것이 아니므로 세 번째 항목에 넣지 말고, 굳이 쓴다면 첫 번째 항목의 맥락으로 적는 편이 정확하다.
